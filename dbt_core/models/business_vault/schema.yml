version: 2

models:
  - name: pit_customer
    description: "Point-in-Time table for Customer."
    columns:
      - name: hk_customer
        tests: [not_null]
      - name: sdts
        description: "Snapshot Date Time Stamp from as_of_date"
        tests: [not_null]
      - name: load_ts_sat_customer_details
        description: "Reference to the customer details satellite."
      - name: load_ts_sat_customer_marketing
        description: "Reference to the marketing satellite."