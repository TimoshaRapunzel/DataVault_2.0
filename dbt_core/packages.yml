packages:
  - package: dbt-labs/dbt_utils
    version: "1.1.1"

  # Скачиваем пакет напрямую с официального GitHub разработчиков
  - git: "https://github.com/ScalefreeCOM/datavault4dbt.git"
    # revision: "v1.5.0"  <-- Можно раскомментировать и указать тег, если нужна строгая версия, 
    # но без этой строки dbt просто скачает самую свежую и стабильную версию (main).